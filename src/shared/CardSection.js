import glamorous from 'glamorous-native';
import { spacing, palette } from '../styles/common';

export default glamorous.view({
  borderBottomWidth: 1,
  borderColor: palette.gray.light,
  padding: spacing.sp4,
  backgroundColor: palette.snow
});
